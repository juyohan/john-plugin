---
description: Create a well-structured git commit with a clear, value-focused message
---

# `ce-commit`

> Generate and apply a clear, value-focused commit message following Conventional Commits format.

$ARGUMENTS

---

Execute the **`ce-commit`** skill. Read `skills/ce-commit/SKILL.md` and follow the instructions exactly. Pass `$ARGUMENTS` as additional context or scope hints.

**Workflow position:** `/ce-code-review` → `/ce-commit` → (optional) push + PR
