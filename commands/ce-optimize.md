---
description: Run metric-driven optimization loops — measure, experiment, converge on best solution
---

# `ce-optimize`

> Define measurable goals, build measurement scaffolding, run parallel experiments, converge on the best solution.

$ARGUMENTS

---

Execute the **`ce-optimize`** skill. Read `skills/ce-optimize/SKILL.md` and follow the instructions exactly. Pass `$ARGUMENTS` as the optimization spec YAML path or optimization goal description.

**Saves output to:** `docs/work/YYYY/MM/DD-<title>.md`
